# jukebot

## installation
run the following in the root directory
    
    npm install

## running
run the following in the root directory
    
    npm run dev
    
the app will be served to localhost:8080
